name: Render Shorts Video

on:
  workflow_dispatch:
    inputs:
      job_id:
        required: true

jobs:
  render:
    runs-on: ubuntu-latest

    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          pip install google-api-python-client google-auth

      - name: Prepare folders
        run: |
          mkdir -p input clips output
          echo '${{ secrets.GCP_SERVICE_ACCOUNT }}' > service-account.json

      - name: Download images from Google Drive temp
        run: |
          python - <<'PY'
          from google.oauth2 import service_account
          from googleapiclient.discovery import build
          from googleapiclient.http import MediaIoBaseDownload
          import io, os, re

          JOB_ID = "${{ github.event.inputs.job_id }}"
          TEMP_FOLDER_ID = "${{ secrets.TEMP_FOLDER_ID }}"

          SCOPES = ["https://www.googleapis.com/auth/drive"]
          creds = service_account.Credentials.from_service_account_file("service-account.json", scopes=SCOPES)
          drive = build("drive", "v3", credentials=creds)

          query = f"'{TEMP_FOLDER_ID}' in parents and trashed=false and name contains '{JOB_ID}_scene_'"
          results = drive.files().list(q=query, fields="files(id,name)", pageSize=1000).execute()
          files = results.get("files", [])

          if not files:
              raise Exception(f"No scene images found for job_id={JOB_ID}")

          def scene_number(name):
              m = re.search(r"_scene_(\d+)", name)
              return int(m.group(1)) if m else 999999

          files.sort(key=lambda f: scene_number(f["name"]))

          with open("downloaded_files.txt", "w") as log:
              for idx, f in enumerate(files, start=1):
                  path = f"input/scene{idx}.png"
                  request = drive.files().get_media(fileId=f["id"])
                  fh = io.FileIO(path, "wb")
                  downloader = MediaIoBaseDownload(fh, request)
                  done = False
                  while not done:
                      status, done = downloader.next_chunk()
                  log.write(f'{f["id"]},{f["name"]}\n')

          with open("image_count.txt", "w") as f:
              f.write(str(len(files)))
          PY

      - name: Create silent audio
        run: |
          IMAGE_COUNT=$(cat image_count.txt)
          DURATION=$((IMAGE_COUNT * 3))
          ffmpeg -f lavfi -i anullsrc=r=44100:cl=mono -t "$DURATION" -q:a 9 -acodec libmp3lame input/audio.mp3

      - name: Create image clips
        run: |
          IMAGE_COUNT=$(cat image_count.txt)
          > clips/list.txt

          for i in $(seq 1 $IMAGE_COUNT); do
            ffmpeg -y \
              -loop 1 -t 3 -i "input/scene${i}.png" \
              -vf "scale=1080:1920:force_original_aspect_ratio=increase,crop=1080:1920,setsar=1" \
              -r 30 \
              -c:v libx264 \
              -pix_fmt yuv420p \
              "clips/clip${i}.mp4"

            echo "file 'clip${i}.mp4'" >> clips/list.txt
          done

      - name: Concatenate clips and add audio
        run: |
          ffmpeg -y \
            -f concat -safe 0 -i clips/list.txt \
            -i input/audio.mp3 \
            -map 0:v -map 1:a \
            -shortest \
            -c:v libx264 \
            -pix_fmt yuv420p \
            -c:a aac \
            output/short_${{ github.event.inputs.job_id }}.mp4

      - name: Upload video to Google Drive videos
        run: |
          python - <<'PY'
          from google.oauth2 import service_account
          from googleapiclient.discovery import build
          from googleapiclient.http import MediaFileUpload

          JOB_ID = "${{ github.event.inputs.job_id }}"
          VIDEOS_FOLDER_ID = "${{ secrets.VIDEOS_FOLDER_ID }}"

          SCOPES = ["https://www.googleapis.com/auth/drive"]
          creds = service_account.Credentials.from_service_account_file("service-account.json", scopes=SCOPES)
          drive = build("drive", "v3", credentials=creds)

          file_path = f"output/short_{JOB_ID}.mp4"
          metadata = {
              "name": f"short_{JOB_ID}.mp4",
              "parents": [VIDEOS_FOLDER_ID]
          }

          media = MediaFileUpload(file_path, mimetype="video/mp4", resumable=True)
          uploaded = drive.files().create(
              body=metadata,
              media_body=media,
              fields="id,name,webViewLink"
          ).execute()

          print(uploaded)
          PY

      - name: Delete temp images for this job
        run: |
          python - <<'PY'
          from google.oauth2 import service_account
          from googleapiclient.discovery import build

          TEMP_FOLDER_ID = "${{ secrets.TEMP_FOLDER_ID }}"

          SCOPES = ["https://www.googleapis.com/auth/drive"]
          creds = service_account.Credentials.from_service_account_file("service-account.json", scopes=SCOPES)
          drive = build("drive", "v3", credentials=creds)

          with open("downloaded_files.txt", "r") as f:
              for line in f:
                  file_id = line.split(",", 1)[0].strip()
                  if file_id:
                      drive.files().delete(fileId=file_id).execute()
          PY
